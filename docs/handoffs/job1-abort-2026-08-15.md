# JOB 1 abort handoff (2026-08-15)

Status: ABORTED. Not PASS. IMPLEMENTATION_READY: NO. PRE_DEPLOYMENT_ACCEPTANCE: not claimed. JOB 2 (Mint policy / install / activate) was never opened. Mint stayed read-only the whole time.

Repo: composer-assistant (Project Ashley), GitHub XharvaK/project-ashley
Handoff HEAD: 243d5a2e3865cd6a96fc3f96f14cdf6c843cc51b (should be origin/master at abort)

Destination that was not reached: Ashley functioning autonomously on the Mint host. This crew stopped on pre-deploy verification, not on deploy.

## What is on master (keep)

These landed and were pushed. They are the latest product state. They are not a green gate.

1. Hidden-file provenance (JOB 1 A–C). Discarded a dirty `startswith(".")` skip. Hidden runtime files are included by default. Production hidden allowlist is empty. Unexpected hidden artifacts fail closed. Tests in `apps/sandbox-broker/src/install-provenance.test.ts`. Script: `deploy/linux-mint/sandbox/install-provenance.py`.

2. Install crash recovery / systemd (JOB 1 A–C). `deploy/linux-mint/sandbox/install.sh`: composed EXIT traps so `on_exit` still runs during COMMIT; `systemctl daemon-reload` and `systemctl enable --now ashley-exec-broker.socket` moved into the COMMITTING window (after publish, before `TX_STATE=COMMITTED`). Crash-window tests in `apps/sandbox-broker/src/install-dirty-source.test.ts`.

3. Docs honesty. `docs/handoffs/mint-corrections-2.md` was corrected so SHA / origin / WORKTREE_CLEAN / live-witness claims stop lying. Live witness remains unimplemented (`LIVE_WITNESS_IMPLEMENTED: NO`).

4. Broker runner proper-fix (61fa012d7b89726bec9ce8c5c0a9b090d0536eb4). Dirty-source suite runs in its own vitest process:
   - new `apps/sandbox-broker/vitest.dirty-source.config.ts` (include only `src/install-dirty-source.test.ts`)
   - `vitest.config.ts` excludes that file
   - `apps/sandbox-broker/package.json` `"test"` is `vitest run --config vitest.dirty-source.config.ts && vitest run`
   - `install-dirty-source.test.ts` replaced `spawnSync` with async `spawn` (`run()`) so the vitest worker event loop is not blocked
   Worker reported split runs green on this SHA. Combined `npm test` at repo level was not Qualifier-certified.

5. Broker recipe follow-up (ac50688a338dbfee6c2bbb65ee9fb3036783617f, parent 61fa012d7b89726bec9ce8c5c0a9b090d0536eb4). Four paths only. Reviewer DIFF_HOLDS. Qualifier pins: 10/10 and 6/6, both exit 0.
   - `test:sandbox-broker-vitest` argv unchanged: `exec --prefix apps/sandbox-broker -- vitest run`
   - description: `run the sandbox-broker rest suite via vitest.config.ts`
   - added `test:sandbox-broker-dirty-source-vitest` argv: `exec --prefix apps/sandbox-broker -- vitest run --config apps/sandbox-broker/vitest.dirty-source.config.ts`
   - description: `run install-dirty-source.test.ts via vitest.dirty-source.config.ts`
   - `R4006_ENGINEERING_RECIPE_IDS` includes the new id immediately after `test:sandbox-broker-vitest`
   Why: after the process split, the existing broker recipe (`vitest run` via default config) silently skipped the crash suite.

6. Agent-service two-shard scripts (243d5a2e3865cd6a96fc3f96f14cdf6c843cc51b, parent ac50688a338dbfee6c2bbb65ee9fb3036783617f). One file: `apps/agent-service/package.json`. Reviewer DIFF_HOLDS. Worker did not run the tests.
   - `"test": "vitest run --maxWorkers=1 --minWorkers=1 --shard=1/2 && vitest run --maxWorkers=1 --minWorkers=1 --shard=2/2"`
   - `"test:offline"`: same, with `--config vitest.offline.config.ts` on both halves
   - `"test:watch": "vitest"`
   This did **not** fix the failure. See below.

## Failure that stopped the job

Vitest prints passes, then dies on `onTaskUpdate` (RPC timeout / dead worker), process exit 1. That is not a slow test. Timeout bumps do not fix it.

Last observed signal (Qualifier, after 243d5a2e3865cd6a96fc3f96f14cdf6c843cc51b landed):

Correction. IMPLEMENTATION_READY NO. Not PASS. onTaskUpdate is s1-only. s2 clean. Two-shard split did not make the parent green.

HEAD at start 243d5a2e3865cd6a96fc3f96f14cdf6c843cc51b. After test:offline still 243d5a2e3865cd6a96fc3f96f14cdf6c843cc51b. After npm test s2: 6fab27a39e340c39f5e52523f8569a033aa5bcf6 (the abort handoff commit).

test:offline parent EXIT 1
- s1 FAIL — 67 files, 644 passed / 2 skipped, 1 error, onTaskUpdate, 05:13:00, 268.50s (ExitCode unread; printed Errors 1)
- s2 EXIT 0 — 66 files, 384 passed, no onTaskUpdate, 05:18:23, 178.91s

npm test parent EXIT 1
- s1 FAIL — 67 files, 644 passed / 2 skipped, 1 error, onTaskUpdate, 05:18:10, 286.37s (ExitCode unread; printed Errors 1)
- s2 EXIT 0 — 66 files, 384 passed, no onTaskUpdate, 05:23:25, 169.82s

## Attempts, in order (do not repeat the failed ones)

1. Packet that looked green. Original pre-deployment write-up claimed a coherent SHA, clean worktree, and a run verification table that had not actually been run. GPT pass of that packet listed SHA/worktree lies, hidden-file provenance skip, unproven crash recovery, systemd after COMMITTED, never-run verification. That is why JOB 1 existed.

2. JOB 1 source slice A–C (provenance, install.sh recovery, docs honesty). Landed. Mint still read-only. Did not make `npm test` honestly green.

3. Timeout bumps (e2a1a5e9e123ff5f30f48ada0af4f87bb8be3e32). `bubblewrap-qualification-harness` clean-checkout `it(..., 60_000)`; split `install-dirty-source` mega-it into timed `it()` blocks. Clean-checkout passed in 2562ms. Dirty-source 13 passed ~216s, then exit 1 `onTaskUpdate`. **Wasted lap.** Raising timeouts and splitting tests does not unstick a dead vitest worker.

4. Proper-fix: process split + async spawn (61fa012d7b89726bec9ce8c5c0a9b090d0536eb4). Necessary for the broker crash suite (spawnSync was blocking the worker). Not sufficient for agent-service. Anti-circle rule written here: if `onTaskUpdate` remains after this, stop; do not add another timeout.

5. Recipe follow-up (ac50688a338dbfee6c2bbb65ee9fb3036783617f). Necessary so broker policy does not skip dirty-source. Pins proven. Does not address agent-service reporter death.

6. Agent-service `--shard=1/2 && --shard=2/2` with `--maxWorkers=1 --minWorkers=1` (243d5a2e3865cd6a96fc3f96f14cdf6c843cc51b). **Failed.** First half still dies on `onTaskUpdate` after printed passes. Do not do `1/2 → 1/N` next. That is the same lap.

7. Not implemented (Architect was scoping when abort hit): a *different* honest-exit mechanism (reporter / worker-pool / process isolation that is not another shard split and not a timeout bump). No lock existed. No Worker write. Do not guess a patch from this paragraph.

## What not to do

- Do not bump vitest / `it()` timeouts to chase `onTaskUpdate`.
- Do not add more `--shard` splits of the same `&&` shape.
- Do not treat printed passed counts as the process result. Exit code and `onTaskUpdate` are the result.
- Do not run `02C`, `install.sh --apply`, policy issuance, activate, or live witness as a substitute for a green suite. Mint was never mutated.
- Do not claim `test:sandbox-broker-vitest` covers dirty-source. It does not. Use `test:sandbox-broker-dirty-source-vitest`.
- Do not use `&&` inside broker recipe argv (`shell_metachar_forbidden`). Local `package.json` scripts may use `&&`; broker recipes may not.
- Do not invent a live witness. `LIVE_WITNESS_IMPLEMENTED: NO`. status.sh / systemctl / curl /health are not a live witness.

## Remaining acceptance gates (from the original packet)

Still open at abort:

- Honest full verification table (repo `npm test`, `npm run test:offline`, `npm run phase0:offline`, plus focused suites). Last agent-service offline half-run was not green.
- `IMPLEMENTATION_READY` / `PRE_DEPLOYMENT_ACCEPTANCE` remain NO / unclaimed.
- Live witness still does not exist.
- JOB 2 never started: Mint policy issuance, install, activate.
- Worktree at abort still had untracked research docs (not part of these commits). `WORKTREE_CLEAN` was not true.

Proven CLI contracts (do not regress; derive from source, do not copy old runbooks blindly):

- 02C is not `sudo deploy/.../run-02c.sh`. Canonical shape: `cd` repo root, `/usr/bin/bash deploy/linux-mint/sandbox/qualification/run-02c.sh <SHA> <repo-root>`, with owner `sudo -v` / `sudo -n true` first and bounded internal sudo in the harness.
- Installer and policy issuance have exact host invocations in the corrected handoff (`docs/handoffs/mint-corrections-2.md`). Do not issue or install from this abort note.

## Qualifier evidence that did land

| Command | SHA | Result | Notes |
|---|---|---|---|
| vitest run src/policy/build-test-recipes.test.ts | ac50688a338dbfee6c2bbb65ee9fb3036783617f | PASS 10/10 exit 0 | recipe pins |
| vitest run src/crypto/delegated-policy-issuance-r4006.test.ts | ac50688a338dbfee6c2bbb65ee9fb3036783617f | PASS 6/6 exit 0 | R4006 pin |
| test:offline parent | 243d5a2e3865cd6a96fc3f96f14cdf6c843cc51b | EXIT 1 | s1 FAIL onTaskUpdate 67 files 644 passed / 2 skipped 1 error 05:13:00 268.50s; s2 EXIT 0 66 files 384 passed 05:18:23 178.91s |
| npm test parent | 243d5a2e3865cd6a96fc3f96f14cdf6c843cc51b start; 6fab27a39e340c39f5e52523f8569a033aa5bcf6 after s2 | EXIT 1 | s1 FAIL onTaskUpdate 67 files 644 passed / 2 skipped 1 error 05:18:10 286.37s; s2 EXIT 0 66 files 384 passed 05:23:25 169.82s |

## Crew / process (only if you care why this took hours)

Locked pipeline: Scout recon → Architect scopes → Reviewer attacks plan → Doc ownerApproval if source-changing → Worker writes → Reviewer attacks diff → Qualifier proves. Cursor cloud agents were off (credits until 2026-08-21). JOB 2 was gated on JOB 1 actually being green, not on a packet that looked green. That gate was never met.
