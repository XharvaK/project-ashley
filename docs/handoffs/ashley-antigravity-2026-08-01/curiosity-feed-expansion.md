# Curiosity feed expansion (Wave 3)

- Original: 16 feeds (`version: 1`)
- Expanded: 64 feeds in `config/curiosity-sources.json` (`version: 2`)
- Strategy: keep originals; add ~3 related feeds per interest area (dev, pharma, music, gaming, philosophy, turkey, wildcard)

Review offline; remove any dead/bot-walled URLs by setting `enabled=0` in `cur_sources` or deleting from JSON before next seed.
