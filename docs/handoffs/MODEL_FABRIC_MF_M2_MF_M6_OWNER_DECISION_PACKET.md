# Model Fabric MF-M2–MF-M6 — owner decision packet (Pass 1)

**Status:** `OWNER CLOSED` — Pass-2 answers recorded 2026-08-25.

**Date:** 2026-08-25

**Planning worktree:** `C:\Users\Xharv\Projects\model-fabric-m2-m6-planning`

**HEAD:** `d915af86483e2af4f5edf2838023ffe22f875dcc`

This packet's questions are closed. Exact contracts:
[`../architecture/Model_Fabric_MF_M2_MF_ACT_Implementation_Contracts.md`](../architecture/Model_Fabric_MF_M2_MF_ACT_Implementation_Contracts.md)

Do not treat listed Pass-1 defaults as chosen. The owner answers in the
Pass-2 prompt are the closed values.

Canonical prior packet:
[`MODEL_FABRIC_OWNER_DECISION_PACKET.md`](MODEL_FABRIC_OWNER_DECISION_PACKET.md)

Research:
[`../architecture/research/Model_Fabric_MF_M2_MF_M6_Research_Audit.md`](../architecture/research/Model_Fabric_MF_M2_MF_M6_Research_Audit.md)

Gap matrix:
[`MODEL_FABRIC_MF_M2_MF_M6_CONTRACT_GAP_MATRIX.md`](MODEL_FABRIC_MF_M2_MF_M6_CONTRACT_GAP_MATRIX.md)

```text
OWNER ANSWERS CLOSED 2026-08-25.
PASS 2 CONTRACTS ARE THE EXECUTION AUTHORITY BENEATH MODEL FABRIC ARCHITECTURE.
```

---

## Worktree

| | |
|---|---|
| Path | `C:\Users\Xharv\Projects\model-fabric-m2-m6-planning` |
| Branch | `model-fabric-m2-m6-planning` |
| Starting SHA | `d915af86483e2af4f5edf2838023ffe22f875dcc` |
| Runtime edits | none |
| Push / Mint | none |

---

## Already closed (do not re-answer)

Owner packet 2026-08-25 #1–#25 remain closed, including:

- MF-M1 is the existing-route seam with zero intended behavior change.
- §12.9 is **target policy**, not live routing.
- Expression **target** Qwen primary / Mistral fallback; **live** remains Mistral primary.
- Thought **target** Groq 120B + NIM 120B same-model failover, desired `high`.
- Curiosity secondaries unordered until you order them (open G below as Q16).
- Evaluation owns `QualificationResult`. Fabric owns mechanical binding.
- No auto-promotion. Track A ≠ Track B. Thought/Expression not on OpenCode initially.
- `ReasoningPolicy` on route policy, not on the capability card.
- Receipt levels: attempt / invocation / chain.

MF-M1 residual receipt repairs R1/R2 are **not** owner architecture questions.
They are closed-contract conformance. See research audit §24.

---

## Historical Pass-1 question restatement (closed by Pass 2)

These questions are **closed**. The text below is the Pass-1 restatement
that the owner answered. Do not treat it as still open.

| Old | New |
|---|---|
| A Track A transport | Q17 |
| B corpora/thresholds | folded into Q4/Q6 (do not invent numbers here) |
| C seats production-active | Q18 |
| D catalog cadence | Q15 |
| E independence enforcement | Q10 |
| F vendor strings | not re-asked as architecture; Pass 2 will copy §12.9 names and leave wire IDs as policy data you can correct |
| G curiosity secondary order | Q16 |
| H §12.9 activation | Q2 |

Exact numeric eval thresholds (old B) stay Evaluation-owned. Pass 1 will not
ask you to pick a score.

---

## MF-M1 conformance (Pass-1 finding; not a vote)

```text
MF-M1 IMPLEMENTATION CONFORMANCE = REPAIR_REQUIRED   (Pass-1)
MF-M1 REPAIR PACKAGE = READY
```

Owner accepted R1/R2 as **SLICE 0**. Two NONBLOCKING receipt-truth
defects. No routing regression. Repair before receipts are used as
qualification evidence. Not an architecture reopen.

---

## Owner questions

### Q1. Constitution `## Model` versus later routing law

**QUESTION**

Constitution Fixed Constraints still say: Mistral Medium; there is currently
no fallback; do not change models merely because another performs better.
Live source already has Mistral→Qwen Expression substitution. §12.9 reverses
that order and moves Thought off Mistral entirely. Hierarchy puts Constitution
above architecture. `SC-CON-04` requires consultation for model-family changes.

How should later Fabric contracts treat that Constitution block?

**WHY IT MATTERS**

Without this, Pass 2 cannot honestly write an activation or even a candidate
§12.9 policy without appearing to ignore higher law.

**CURRENT SOURCE / DOCUMENTATION STATUS (Pass-1 then-status)**

- Constitution `## Model` was then unchanged. **Pass-2:** amended; SC-CON-04 Constitution-text consultation recorded. Family-cutover consultation remains separate.
- Live Expression fallback is `existing_compatibility` (MF-M1 preserved).
- §12.9 / owner packet #21–#23 closed as **target**, not activation.
- Pass-1 then had no in-tree Constitution amendment and no recorded `SC-CON-04` artifact for Qwen-primary or GPT-OSS Thought. The Constitution-text consultation now exists; a **family-cutover** SC-CON-04 with live Ashley position still does not.

**OPTION A**

Amend Constitution `## Model` (and record `SC-CON-04` consultation) so it
describes a multi-provider Fabric portfolio, not a single Mistral Medium
with no fallback.

Consequence: higher law matches the already-closed target map. Later
activation is no longer in contradiction.

**OPTION B**

Treat Constitution `## Model` as historical Expression-era constraint that
the 2026-08-25 owner packet already superseded for **target policy**, and
record that supersession in Constitution later, not now.

Consequence: architecture can proceed; the written higher-law contradiction
remains until a later amendment.

**OPTION C**

Keep Constitution as written. §12.9 remains documentation-only forever unless
a future amendment happens. Live fallback is a tolerated compatibility scar
only.

Consequence: M2–M6 may build machinery. Core cutover is forbidden.

**YOUR RECOMMENDATION**

A. The Constitution sentence is factually false against current source
already. Leaving it unamended while implementing Qwen-primary / 120B Thought
will keep producing fake “architecture vs constitution” incidents.

**DEFAULT IF OWNER DOES NOT CARE**

B for Pass 2 drafting only, with an explicit unresolved-higher-law banner.
Do not activate §12.9 under C silently.

**WHICH MF MILESTONE(S) IT BLOCKS**

Any activation of §12.9 (Q2). Not M2 identity unification.

---

### Q2. What is in the M2–M6 Luna program?

**QUESTION**

As numbered today, M2 has no user-visible routing change, M3 is records only,
M4 is optional qualified utility OpenCode, M5 pools still exclude
Thought/Expression, M6 is specialist seats, and §12.9 core cutover is H
(unassigned). Should the upcoming implementation-ready program:

**WHY IT MATTERS**

This is the difference between “build the machine” and “switch Ashley’s
mouth and mind.”

**CURRENT SOURCE / DOCUMENTATION STATUS**

Architecture §34 and roadmap handoff §3. M5 text: still not Thought/Expression.

**OPTION A**

Machinery only. M2–M6 never change live Thought/Expression. §12.9 core
cutover is a later owner-gated slice after qualification + Q1.

**OPTION B**

Machinery plus a named **MF-ACT** (or similar) slice in the same Luna goal
that can activate already-qualified, owner-approved §12.9 roles one at a
time. Still fail-closed. Still not auto-promote.

**OPTION C**

Expand M5/M6 to include Thought/Expression cutover without a new ID.

**YOUR RECOMMENDATION**

A for the numbered M2–M6 contracts, with B only if you want one autonomous
goal to also flip production routes after you separately qualify and
approve. Do not pick C. C overloads M5.

**DEFAULT IF OWNER DOES NOT CARE**

A.

**WHICH MF MILESTONE(S) IT BLOCKS**

All of M2–M6 scope text; H; Pass 2 contract set.

---

### Q3. Policy version grain

**QUESTION**

When a target or current route table is versioned, is the revision identity
per logical role, for the whole portfolio, or both (portfolio revision
containing per-role rows)?

**WHY IT MATTERS**

Rollback blast radius, qualification binding, and whether Expression can
move while Thought stays.

**CURRENT SOURCE / DOCUMENTATION STATUS**

No `RoutePolicyRevision`. Split live authority. §12.9 is one table.

**OPTION A**

Whole-portfolio revision only. Any role change publishes a new full table.

Consequence: simple rollback. Cannot qualify/activate one role without
rebinding the rest.

**OPTION B**

Per-role revisions only.

Consequence: independent rollout. Easy to forget a cross-role fallback
(Thought failover sharing a Groq bucket with utility).

**OPTION C**

Both: a portfolio revision hash that contains explicit per-role rows.
Qualification binds role+row. Activation may enable a subset of rows.
Rollback names either a role row or a whole portfolio pointer.

**YOUR RECOMMENDATION**

C.

**DEFAULT IF OWNER DOES NOT CARE**

C.

**WHICH MF MILESTONE(S) IT BLOCKS**

M2 (CURRENT snapshot), M3 (qualification binding), M5/M6 (activation/rollback).

---

### Q4. What exactly is qualified?

**QUESTION**

What is the subject of a model `QualificationResult` that later routing may
cite?

**WHY IT MATTERS**

Wrong grain either double-qualifies forever or transfers evidence illegally
(Thought onto failover, Expression onto Qwen, Lightning onto another seat).

**CURRENT SOURCE / DOCUMENTATION STATUS**

Evaluation Plane: route-specific; occupant × seat; three bindings (profile,
dispatch, campaign). Fabric §15: same model + material inference-policy may
need a new result. Transfer prohibitions are closed. Grain of “route policy
vs fingerprint vs chain vs portfolio” is not named as a single enum.

**OPTION A**

Profile fingerprint only (provider+model+mechanical card).

Consequence: too coarse. Qwen `none` vs `default` would collide unless
fingerprint includes inference policy (it should). Still would not capture
role.

**OPTION B**

Role + inference fingerprint (provider, model, material generation/reasoning,
structured output). Fallback hops need their own results. A chain is not
qualified as one blob.

**OPTION C**

Whole fallback chain or whole portfolio as one result.

Consequence: one fail blocks everything; one pass over-authorizes.

**YOUR RECOMMENDATION**

B. Campaign binding stays Evaluation-owned and must not enter the profile
fingerprint. Optionally a **portfolio report** can cite many B results
without being a transferable qualification.

**DEFAULT IF OWNER DOES NOT CARE**

B.

**WHICH MF MILESTONE(S) IT BLOCKS**

M3, Evaluation spike, any later activation.

---

### Q5. May an unqualified fallback run live?

**QUESTION**

If the primary is qualified and approved, but the fallback fingerprint is
not, may production dispatch the fallback?

**WHY IT MATTERS**

Today Thought Groq 20B failover and Expression Qwen hop are
`existing_compatibility`, not qualification. After leaving compatibility,
this decides silence vs unauthorized substitution.

**CURRENT SOURCE / DOCUMENTATION STATUS**

Architecture §18: unqualified substitution never. Owner #6: substitution only
among owner-approved candidates. Emergency exception is not written.

**OPTION A**

Never. Unqualified fallback does not run. Role fails closed (existing Thought
floor / Expression fail).

**OPTION B**

Never for model substitution. Same-model **transport** failover may run if
the **same** inference fingerprint is already qualified on the other
provider (provider is part of fingerprint, so this still needs a second
result unless you carve transport as same-fingerprint-except-provider).

**OPTION C**

Owner-named emergency occupants may run unqualified for availability, logged
as degraded, never silently becoming canonical policy.

**YOUR RECOMMENDATION**

A for substitution. For Thought same-model transport, require a second
qualification of the other provider (B’s transport carve) rather than C.
Do not let outage rewrite §12.9.

**DEFAULT IF OWNER DOES NOT CARE**

A, plus same-model transport only when that provider+fingerprint is
separately qualified (the Evaluation Plane already says Thought qualification
does not transfer to failover).

**WHICH MF MILESTONE(S) IT BLOCKS**

M3, M5, any §12.9 Thought/Expression activation.

---

### Q6. What invalidates qualification?

**QUESTION**

Which changes require a new `QualificationResult` rather than reusing one?

Assume already closed: provider, model id, role/seat, material reasoning,
temperature, max-output, structured output, privacy class, fallback topology
that changes substitution.

Confirm or reject the extras:

1. Adapter code change (hash of adapter module / dispatch mapping)
2. ContextProjection schema/version / content-binding policy
3. System / identity prompt version
4. Route-policy revision id (even if fingerprint unchanged)
5. Provider-reported resolved model id drift
6. Fallback **order** change among already-qualified occupants

**WHY IT MATTERS**

Too tight: endless requalification. Too loose: stale evidence on a different
mind.

**CURRENT SOURCE / DOCUMENTATION STATUS**

Evaluation identity-continuity epoch lists provider, resolved model, context
profile, identity prompt, fallback policy, material reasoning, rubric, corpus.
Adapter-code hash is not named. Projection version is not named as a Fabric
field.

**OPTION A**

Invalidate on all six extras plus the already-closed set.

**OPTION B**

Invalidate on prompt/context-profile/fallback-policy/resolved-model-drift
(Evaluation already), plus material fingerprint. Do **not** auto-invalidate
on adapter-file hash unless mechanical translation of reasoning/options
changed (that change already hits fingerprint).

**OPTION C**

Invalidate only on fingerprint and role. Prompt changes are Identity/Expression
problems, not model qualification.

**YOUR RECOMMENDATION**

B. Prompt and context-profile changes already have an identity-continuity
epoch. Adapter churn without wire-behavior change should not nuke evidence.
Fallback **order** among qualified occupants is policy revision (Q3), not
automatically a new behavioral qualification unless an occupant is new.

**DEFAULT IF OWNER DOES NOT CARE**

B.

**WHICH MF MILESTONE(S) IT BLOCKS**

M3; Evaluation spike; M5 pools.

---

### Q7. Interactive Thought latency envelope

**QUESTION**

What envelope must core Thought meet after a 120B `high` cutover is even
eligible to consider?

Current reactive `initialThoughtMs` is 6000. Output cap is 1000 tokens.
Proactive Thought has no deadline. Durable Thought uses remaining job
lifetime.

**WHY IT MATTERS**

Packet #23 forbids `max_supported` until interactive latency qualifies.
On GPT-OSS, `high` **is** max wire effort. So this question is really:
may interactive Thought use Groq/NIM 120B `high` inside **this** envelope,
or must the envelope change first?

**CURRENT SOURCE / DOCUMENTATION STATUS**

`turn-deadline-plan.ts` 387; `thought.ts` `THOUGHT_MAX_OUTPUT_TOKENS = 1000`.
No empirical 120B `high` number in-repo. Groq free TPM 8K is an external
current fact, not an envelope.

**OPTION A**

Keep 6000 ms / 1000 output tokens. 120B `high` must empirically fit or it
does not activate. Failover floor stays 2500 ms remaining.

**OPTION B**

Raise the interactive Thought deadline (you name the number in Pass 2) so
`high` 120B is allowed to be slower than 20B `low`.

**OPTION C**

Split: reactive stays tight; durable/proactive may use 120B `high` first;
interactive stays 20B until A or B is proven.

**YOUR RECOMMENDATION**

C as activation order if Q2 includes cutover; A as the hard interactive
gate. Do not pick a new millisecond number in Pass 1. Empirical
qualification owns the pass/fail.

**DEFAULT IF OWNER DOES NOT CARE**

A for interactive eligibility; C for rollout order if you later activate.

**WHICH MF MILESTONE(S) IT BLOCKS**

§12.9 Thought activation (Q2). Not M2.

---

### Q8. Who may request `high` vs `max_supported`?

**QUESTION**

For roles whose **target** is `high` or `max_supported`, who is allowed to
choose the value on a given invocation?

Packet #23: Thought desired `high`; `max_supported` only after interactive
latency qualifies. Prompt text said “high normally, max when consequence
justifies it.” Those are different.

For GPT-OSS they collapse on the wire. For Glimmer, `xhigh` is distinct.
For Nemotron, thinking on/off is distinct from GPT-OSS enums.

**WHY IT MATTERS**

If Fabric picks “this turn looks hard,” Fabric becomes a cognition owner.

**CURRENT SOURCE / DOCUMENTATION STATUS**

Attention does not own desired reasoning. Thought owns cognitive effort
allocation. Route policy owns default `ReasoningPolicy`. Live Thought
hardcodes `low`.

**OPTION A**

Role policy default only. No per-turn raise. Thought interactive = `high`
once qualified. Background Ultra roles = `max_supported` always.

**OPTION B**

Role default + Thought may request a raise within that role’s allowed set.
Attention never chooses it. Fabric only translates.

**OPTION C**

A later consequence classifier / Attention budget chooses max vs high.

**YOUR RECOMMENDATION**

A until a real consequence classifier exists. B later if Thought already
has an effort field you want to map. Not C. Not Fabric.

**DEFAULT IF OWNER DOES NOT CARE**

A.

**WHICH MF MILESTONE(S) IT BLOCKS**

M2 translation tables; §12.9 Thought/observation/reflection; M6 seats.

---

### Q9. Evaluation seats versus Fabric routes

**QUESTION**

When independent judge / adversarial second judge occupants (Inkling,
Glimmer) run, who owns the seat?

**WHY IT MATTERS**

Duplicate “judge” routing inside Fabric will fight Evaluation.

**CURRENT SOURCE / DOCUMENTATION STATUS**

§12.9 lists those occupants. Evaluation Plane owns judges inside
`EvaluationDefinition` / campaign binding. Fabric may resolve mechanical
models.

**OPTION A**

Evaluation chooses semantic seat + campaign. Fabric only resolves an
already-approved occupant for that campaign’s requested role/seat.

**OPTION B**

Fabric route policy includes `evaluation_judge` as a normal production role
like Thought.

**OPTION C**

Both independently, hoping they match.

**YOUR RECOMMENDATION**

A. Production user-visible paths must not silently call Inkling. Judge
invocations are Evaluation (or owner-triggered review), not Discord turns.

**DEFAULT IF OWNER DOES NOT CARE**

A.

**WHICH MF MILESTONE(S) IT BLOCKS**

M3 packs; M6; Evaluation spike.

---

### Q10. What counts as an independent judge? When is dual review mandatory?

**QUESTION**

`independence_group` is representable. High-impact identity or model-family
qualification already requires at least one materially independent judge.
What is “materially independent,” and when is dual-model review mandatory
outside those campaigns?

**WHY IT MATTERS**

Same-family agreement is not independent evidence (closed). Without a
definition, M6 review seats are theatre.

**CURRENT SOURCE / DOCUMENTATION STATUS**

Owner packet #15 and E. Evaluation Plane: same-family may contribute, not
sole high-impact judge. Evaluation decision #4 still open (which
families count). Not required for every task. No permanent council.

**OPTION A**

Independence = different `independence_group` (catalog field you assign:
e.g. `openai_gpt_oss`, `nvidia_nemotron`, `meta_muse`, `thinking_machines_inkling`,
`alibaba_qwen`, `minimax`). Mandatory dual review only for high-impact
identity / model-family qualification campaigns (already closed) and for
`architecture_critique` / `adversarial_audit` when those seats are
production-active.

**OPTION B**

Independence requires different provider **and** different family. Dual
review for every Reflection and every Expression change.

**OPTION C**

Independence is “a second model exists.” No group taxonomy.

**YOUR RECOMMENDATION**

A. Assign groups as policy data (old F). Do not require dual review on
ordinary reflection.

**DEFAULT IF OWNER DOES NOT CARE**

A.

**WHICH MF MILESTONE(S) IT BLOCKS**

M3 catalog field; M6; Evaluation campaigns. Old E.

---

### Q11. Durable state: what, and where?

**QUESTION**

Which of these must be durable outside git, and where?

- current route-policy pointer
- candidate policy revisions
- qualification artifacts
- promotion records
- activation pointer
- catalog occupancy
- cooldowns / route health

**WHY IT MATTERS**

A Fabric schema in `nuclear.db` becomes a second authority if we are sloppy.
Git-only cannot crash-survive an activation pointer.

**CURRENT SOURCE / DOCUMENTATION STATUS**

No Fabric tables. Evaluation first spike: no runtime DB, artifacts outside
repo by default. Architecture §25 proposes operator config + `nuclear.db`
health as **proposal**. Evaluation decision #1: artifact home undecided.

**OPTION A**

Git for policy revisions and occupancy definitions. Local user-data files
for `QualificationResult`. Activation = deployed config / env pointer, not
SQLite. Health/cooldowns process-local.

**OPTION B**

New `nuclear.db` Fabric tables for all of the above.

**OPTION C**

Evaluation artifacts in `nuclear.db`; policy in git.

**YOUR RECOMMENDATION**

A. Add a DB pointer later only if Mint activation needs crash-safe
multi-process agreement. Do not start with B.

**DEFAULT IF OWNER DOES NOT CARE**

A.

**WHICH MF MILESTONE(S) IT BLOCKS**

M2–M6 persistence sections; Evaluation spike.

---

### Q12. Promotion and activation approval

**QUESTION**

What exact owner act turns `qualified` into `owner_approved`, and what
exact owner act turns `owner_approved` into production-routed?

**WHY IT MATTERS**

Control-plane boundary. Tests must not activate.

**CURRENT SOURCE / DOCUMENTATION STATUS**

Ladder closed; artifact not. `SC-CON-04` consultation artifact TBD
(Evaluation decision #2). Capability promotion is a different path.

**OPTION A**

Two explicit records you sign (files or commands): `OwnerApprovalRef` citing
`qualificationId` + role/seat, then `ActivationRef` citing the policy
revision + approved roles. Both required. Revocable. Rollback cites prior
`ActivationRef`.

**OPTION B**

One approval implies activation.

**OPTION C**

Green Evaluation `recommendation: PROMOTE` activates.

**YOUR RECOMMENDATION**

A. Always. Including utility OpenCode (M4).

**DEFAULT IF OWNER DOES NOT CARE**

A.

**WHICH MF MILESTONE(S) IT BLOCKS**

M3 records; M4 enablement; M5/M6; H.

---

### Q13. Rollout and rollback unit

**QUESTION**

If Q3 is “both,” what is the product rollout unit you actually want when
something is qualified: one role, a group (cognition / expression /
utility / evaluation), or the whole portfolio?

**WHY IT MATTERS**

Isolation vs consistency (Expression Qwen-primary while Thought still 20B).

**CURRENT SOURCE / DOCUMENTATION STATUS**

Not closed. Isolation favors per-role. Shared Groq buckets couple utility
and Thought failover today.

**OPTION A**

Per logical role, with an explicit coupling list (Thought failover bucket
vs utility) that must be reviewed together.

**OPTION B**

Fixed groups: core (Thought+Expression), support (observation, reflection,
exchange, curiosity), utility, evaluation/specialists.

**OPTION C**

Whole portfolio only.

**YOUR RECOMMENDATION**

A plus the coupling list. Do not require Expression and Thought to move on
the same day.

**DEFAULT IF OWNER DOES NOT CARE**

A.

**WHICH MF MILESTONE(S) IT BLOCKS**

M5/M6 activation; Q2 cutover slice.

---

### Q14. Partial availability inside an approved chain

**QUESTION**

Ultra down, Super up. Qwen down, Mistral up. Inkling down, Glimmer up.
After those chains are approved, does Ashley automatically use the next
approved occupant, refuse the role, or require a new activation?

**WHY IT MATTERS**

Temporary outage must not rewrite canonical target policy (§38). Unauthorized
emergency routing is forbidden.

**CURRENT SOURCE / DOCUMENTATION STATUS**

Live Thought already auto-failovers NIM→Groq 20B among `existing_compatibility`.
§18 allows transport failover and approved substitution only.

**OPTION A**

Automatic within the **already-approved chain** for that role, with receipts
showing which occupant ran. Canonical target row unchanged.

**OPTION B**

Refuse the role unless the primary occupant is healthy.

**OPTION C**

Automatic only for same-model transport; substitution requires a new owner
activation each time.

**YOUR RECOMMENDATION**

A for a pre-approved ordered chain. B for seats with no approved secondary.
Not silent 120B→20B unless 20B is an approved occupant **for that role**
(target text says it must not be for core Thought).

**DEFAULT IF OWNER DOES NOT CARE**

A, with the Thought 20B downgrade forbidden unless you later approve it.

**WHICH MF MILESTONE(S) IT BLOCKS**

M5; §12.9 activation.

---

### Q15. Cadence and quota pressure

**QUESTION**

Two coupled operational policies:

1. Catalog refresh / qualification refresh cadence: on-demand only, or a
   timer?
2. When quota or rate-limit pressure hits, may Fabric automatically pick a
   cheaper approved occupant, or only fail closed / use the pre-approved
   failover chain?

**WHY IT MATTERS**

Cadence can become a hidden job. Quota-chasing can become hidden cognition
and policy drift.

**CURRENT SOURCE / DOCUMENTATION STATUS**

Owner D open. Owner #7: cost cannot promote unqualified; preference among
approved outranks pure cost. Budget exhaustion fails closed (§29).

**OPTION A**

No timer. Catalog refresh is owner- or Network-admitted on demand.
Qualification refresh is owner-admitted when identity changes (Q6).
Quota pressure uses the approved chain only (Q14), never a cheaper
unapproved model, never a rewrite of target policy.

**OPTION B**

Daily catalog probe + automatic move to any cheaper approved occupant.

**OPTION C**

Automatic degraded routing including unqualified occupants when quota bites.

**YOUR RECOMMENDATION**

A.

**DEFAULT IF OWNER DOES NOT CARE**

A.

**WHICH MF MILESTONE(S) IT BLOCKS**

M3 (D), M5, M4 utility offload motivation.

---

### Q16. Curiosity secondary order (old G)

**QUESTION**

Curiosity target primary is Nemotron 3 Super. Secondaries named MiniMax M3
and Groq GPT-OSS 120B with **no order**. What is the order, or do you keep
them unordered until qualification picks one?

**WHY IT MATTERS**

M3 occupancy and later M5 pools cannot implement “a chain” without an order
or an explicit “qualification will order this.”

**CURRENT SOURCE / DOCUMENTATION STATUS**

§12.9 and owner G. Groq currently documents MiniMax **M2.7**, not M3, as a
reasoning model id. That is vendor-string churn (old F), not a reason to
pick by taste.

**OPTION A**

Leave unordered. Qualification of the two secondaries may produce an order
later. Until then, only Super is activatable for that role.

**OPTION B**

You name the order now: e.g. Super → Groq 120B → MiniMax M3 (or reverse).

**OPTION C**

Drop MiniMax from curiosity until a stable paid non-trial id exists. Super
→ Groq 120B only.

**YOUR RECOMMENDATION**

A unless you already care about MiniMax vs 120B. C if you do not want Zen
MiniMax near curiosity synthesis privacy-wise.

**DEFAULT IF OWNER DOES NOT CARE**

A.

**WHICH MF MILESTONE(S) IT BLOCKS**

M3 occupancy for curiosity; M5.

---

### Q17. First OpenCode Track A transport (old A)

**QUESTION**

If M4 happens, what is the first Track A transport?

**WHY IT MATTERS**

Implementation/qualification choice, not identity. Still blocks M4 code.

**CURRENT SOURCE / DOCUMENTATION STATUS**

Owner A. Snapshot: Zen HTTP vs isolated `opencode serve` vs other. Track B
worker is out of this sequence. OC-M0/M1 are off-tree evidence only.

**OPTION A**

Defer M4 transport until M3 records exist; Pass 2 writes M4 as
“transport TBD, fail-closed, utility-only, no tools.” Do not pick HTTP vs
serve now.

**OPTION B**

Zen HTTP chat-completions for utility-only, privacy class excluding
owner-private Thought/Expression (already closed).

**OPTION C**

Isolated `serve` from the start.

**YOUR RECOMMENDATION**

A in this Pass 1. Picking B vs C without a qualification packet repeats
the OpenCode contamination problem. M4 contract can still require
fail-closed absence.

**DEFAULT IF OWNER DOES NOT CARE**

A.

**WHICH MF MILESTONE(S) IT BLOCKS**

M4 only. Must not block M2/M3.

---

### Q18. When specialist seats become production-active (old C)

**QUESTION**

M6 is “where evidence justifies.” What is the first seat that may go live,
and is that even in this Luna program?

**WHY IT MATTERS**

Otherwise M6 is an empty milestone or an unbounded specialist runtime.

**CURRENT SOURCE / DOCUMENTATION STATUS**

Seats named in §12.3–12.5. Engineering currently records
`complex_orchestration` without selection. Production activation is M6.
Owner C open.

**OPTION A**

M6 in this program is **capability only**: resolve a seat when requirement
is present **and** occupant is owner_approved; no seat approved at start.
Engineering stays on current Expression/Mistral until a later approval.
Empty approved set → current compatibility path.

**OPTION B**

Approve one seat in the same program (you name it), e.g. `routine_validation`
on Lightning after utility qualification.

**OPTION C**

Activate the full scouting portfolio (Spark/Hy3/MiMo/Ultra/Lightning) when
M6 ships.

**YOUR RECOMMENDATION**

A. B only if you want a single utility seat as the M4/M6 overlap. Not C.

**DEFAULT IF OWNER DOES NOT CARE**

A.

**WHICH MF MILESTONE(S) IT BLOCKS**

M6; engineering specialist selection; Track A vs seats.

---

## Shadow (not a separate question unless you want it)

Recommendation, not frozen: **no default live dual-run** of user turns.
Evaluation may use artifact campaigns and optional sampled shadow. If you
disagree, say so; otherwise Pass 2 will write that as default for Q2
cutover evidence (E6 remains available, not mandatory for every route).

---

## What Pass 2 will do after you answer

1. Close these rows in a successor owner packet.
2. Write exact M2–M6 contracts (plus MF-ACT only if Q2 = B).
3. Put MF-M1 R1/R2 repair in the Luna `/goal` as slice 0.
4. Still not implement in Pass 2 unless you ask.

End of packet.
