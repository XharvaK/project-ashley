# Models

| File | Source |
|------|--------|
| `orpheus-3b-0.1-ft-q4_k_m.gguf` | `scripts/download-models.ps1` |
| `ashley.onnx` | Train with `scripts/train-wakeword.md` (wake phrase: **Ashley**) |

Whisper `large-v3-turbo` downloads on first voice-service run.

Until `ashley.onnx` exists, say **"Ashley, ..."** (phrase detection) or toggle listen with **Ctrl+Shift+Space** (press once to start, again to send).
